#?'

# NOTE this file has been autogenerated by BKNR.WEB
# do not edit

backend b {
  set backend.host = "localhost";
  set backend.port = "$<backend-port>";
}

sub vcl_recv {
  if (req.http.Cookie) {
    if (req.url ~ "^(/image|/static)") {
         remove req.http.Cookie;		  
    }
  }
}

sub vcl_fetch {
   if (obj.http.Set-Cookie) {
     if (req.url ~ "^(/image|/static)") {
        remove obj.http.Set-Cookie;
     }		  
   }
}


'
