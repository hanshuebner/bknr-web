#?'

# NOTE this file has been autogenerated by BKNR.WEB
# do not edit

backend b {
  .host = "localhost";
  .port = "$<backend-port>";
}

sub vcl_recv {
  if (req.http.Cookie) {
    if (req.url ~ "$<cachable-prefixes-regex>") {
         remove req.http.Cookie;		  
    }
  }
}

sub vcl_fetch {
   if (obj.http.Set-Cookie) {
     if (req.url ~ "$<cachable-prefixes-regex>") {
        remove obj.http.Set-Cookie;
     }		  
   }
}


'
